"use strict";
/*import { connect } from "../../node_modules/react-redux";
import * as setters from "../store/Actions";

const mapStateToProps = state => ({
    currentAnswerRdx: state.main.currentAnswer,
    currentAnswerIdRdx: state.main.currentAnswerId,
    questionIdRdx : state.main.questionId,
    questionRdx : state.main.question
});

const mapDispatchToProps = dispatch => ({
    setCurrentAnswerIdRdx: newSearchValue => dispatch(setters.setcurrentAnswerId(newSearchValue)),
    setCurrentAnswerRdx: ChannelUsersRdx => dispatch(setters.setcurrentAnswer(ChannelUsersRdx)),
    setQuestionIdRdx: QuestionIdRdx => dispatch(setters.setQuestionId(QuestionIdRdx)),
    setQuestionRdx: QuestionRdx => dispatch(setters.setQuestion(QuestionRdx)),
});

export const withSearchValue = connect(
    mapStateToProps,
    mapDispatchToProps
);
*/
